# Environment files (any .env / .env.local / .env.production / etc.)
.env
.env.*
!.env.example
.venv/
.venv*/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/

# Editor
.idea/
.vscode/
*.swp
.DS_Store

# Notebook
.ipynb_checkpoints/

# Local artefacts (everything substantive lives in GCS for this project).
local_cache/
*.tif
*.tiff
*.tfrecord
*.gz

# Per-machine resumable-harvest state (task IDs are user-specific).
.gee_s1s2_harvest_state.json

# Per-run harvest logs.
harvest_*.log

# Service-account JSONs and other credential files — never commit.
*-key.json
service-account*.json
credentials.json
client_secret*.json
application_default_credentials.json
gcloud-*.json
# OAuth tokens, SSH keys, generic PEM/PKCS files.
*.pem
*.key
!*.public.key
*.crt
oauth_token*
.gcloud/
# GEE / Earth Engine credential caches if they ever land in the tree.
.earthengine/
.config/earthengine/

# One-off scratch dirs from the Welsh OOD experiment (Day 3 afternoon).
# Canonical artefacts live in docs/images/ and training/WORKED_EXAMPLE_BERWYN.md.
training/welsh_ood_work/
